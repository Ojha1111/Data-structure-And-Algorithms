#include<stdio.h>
#include<conio.h>
void main()
{
int a[10],b[10],c[20],i,j,x,y,k;
clrscr();
printf("plz enter the size of the first array");
scanf("%d",&x);
printf("Plz enter the elements of the 1st array in an assending order");
for(i=1;i<=x;i++)
{
scanf("%d",&a[i]);
}
printf("plz enter the size of the second array");
scanf("%d",&y);
printf("Plz enter the elements of the 2nd array in an assending order");
for(i=1;i<=x;i++)
{
scanf("%d",&b[i]);
}
for(i=0,j=0,k=0;i<x,j<y,k<x+y;i++,j++,k++)
{
if(a[i]<b[j])
{
c[]
}
}
}